The model card has the following problems:
{% for problem in annotations %}
  * {{severity[problem.severity].emojiString}} `{{problem.jsonPath | join(".")}}`: {{problem.message}}
{% endfor %}
Fix them before you can render the model card.